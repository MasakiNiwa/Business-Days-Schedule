# Business-Days-Schedule

営業日を考慮したカレンダー。

日本の祝日を自動で取り込み、**反復ルール**（毎月25日／月末営業日／第5営業日／第2・第4火曜 など）から
予定を導出し、休祝日にかかった予定を**前営業日・翌営業日へ自動でずらして**表示します。

ルールを持たない単発の予定登録は、意図的に対象外としています。

- 📄 **[仕様書 docs/SPEC.md](docs/SPEC.md)**

**公開先**: https://masakiniwa.github.io/Business-Days-Schedule/

## 状態

**M3.1 完了** — 月カレンダー表示、ルールの追加・編集・削除、営業日カレンダー設定、
JSON 入出力、配色モード切替、ヘルプ、日付の詳細。
残るは一覧（リスト）表示と印刷 CSS の仕上げです。

| | |
|---|---|
| ロジック層 | `src/core/` — 営業日判定・反復ルール展開・営業日補正・検証・入出力 |
| 表示層 | `src/ui/` — 月カレンダー、ルール一覧、編集フォーム、日付詳細、設定、ヘルプ |
| テスト | 232 件（`npm test`）。§6 の実務ユースケース17件を回帰テスト化 |
| 祝日データ | `public/data/holidays.json`（毎週月曜 03:00 JST に自動更新） |

初回は「サンプルを読み込む」から、給与振込・月次締め・第5営業日の請求書発行など
実務でよく使う8件のルールを入れて動きを確認できます。

## 使い方

1. **ルールを追加** — 「毎月N日 / 第N営業日 / 第N曜日 / 毎週」から選び、
   休業日にあたったときの補正（前倒し・後ろ倒し・**前後の両方**・近い方・補正なし）を決めます。
   フォーム下部の**「次の10回」プレビュー**で、実際にどの日になるかをその場で確認できます。
   取引先ごとに前倒し・後ろ倒しが分かれる入金予定などは「前後の営業日の両方へ」を選ぶと、
   1つのルールで両側を押さえられます。
2. **カレンダーを読む** — 補正された予定には `←10` `→10` が付きます。`←` は前倒し、
   `→` は後ろ倒し、数字は補正前の日です。日付をクリックすると当日の予定を全件見られます。
3. **営業日を定義** — 設定（⚙）から、週末の曜日・祝日を休むか・年末年始などの休業期間・
   臨時休業日・祝日出勤日を指定します。「自社」と「銀行」を別々に持てるので、
   社内の締めと振込を使い分けられます。
4. **バックアップ** — 設定の「エクスポート」で JSON に書き出せます。
   データはブラウザの localStorage に保存されるため、端末を移るときは書き出してください。

配色は右上のボタンで 自動 / ライト / ダーク を切り替えられます。
操作に迷ったら **?** ボタンから画面内ヘルプを開けます。

## 祝日データについて

一次ソースは [`holiday-jp/holiday_jp`](https://github.com/holiday-jp/holiday_jp) の `holidays.yml`
（UTF-8、1970〜2050年、振替休日・国民の休日を収録）。
内閣府 CSV は Shift_JIS・掲載範囲が翌年までと扱いにくいため、CI 上での二次検証にのみ使い、
差異を検出した場合は Issue を自動起票します。詳細は [仕様書 §3](docs/SPEC.md#3-祝日データ)。

## 開発

```bash
npm install
npm run dev        # 開発サーバー
npm test           # テスト
npm run typecheck  # 型チェック
npm run build      # 本番ビルド
```

祝日データの手動更新:

```bash
npm run holidays         # holidays.yml → public/data/holidays.json
npm run holidays:verify  # 内閣府 CSV と直近3年分を突合
```
